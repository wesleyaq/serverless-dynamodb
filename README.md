# Serverless Dynamodb

Instalar:
```sh
$ npm install
```

Instalar DynamoDB Local:
```sh
$ sls dynamodb install
```

Correr en Local:
```sh
$ npm start
```

Ruta Local:
```sh
http://localhost:3000/api/users
```

Deploy:
```sh
$ sls deploy
```

Ruta dev:
```sh
{server}/dev/api/users
```

